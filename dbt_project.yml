name: decade
version: 1.0.0
profile: decade
config-version: 2

models:
  decade:
    canonical:
      +materialized: table
      staging:
        +materialized: view
    consumption:
      +materialized: view
